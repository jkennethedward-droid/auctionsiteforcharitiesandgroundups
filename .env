# This repo contains a Next.js app in the `web/` folder.
#
# Do NOT put secrets in this file. For local Next.js dev, use `web/.env.local`.
# For production, set variables in Vercel → Project → Settings → Environment Variables.